version: '3.8'

services:
  localstack:
    image: localstack/localstack:1.4.0
    ports:
      - "4566:4566"
    environment:
      - SERVICES=dynamodb,sqs,lambda
      - DEBUG=1
      - DATA_DIR=/tmp/localstack/data
      - DOCKER_HOST=unix:///var/run/docker.sock
      - LAMBDA_EXECUTOR=docker
      - AWS_DEFAULT_REGION=us-east-1
    volumes:
      - "./localstack-init:/etc/localstack/init/ready.d"
      - "/var/run/docker.sock:/var/run/docker.sock"
    networks:
      - backend

  compiledlayoutservice:
    build:
      context: .
      dockerfile: src/AdaptiveRemote.Backend.CompiledLayoutService/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      # Set Cognito dev user pool values here or via a .env file.
      # See _doc_Auth.md for Cognito dev user pool setup instructions.
      - Cognito__Authority=${COGNITO_AUTHORITY:-}
      - Cognito__Audience=${COGNITO_AUDIENCE:-}
    networks:
      - backend

  rawlayoutservice:
    build:
      context: .
      dockerfile: src/AdaptiveRemote.Backend.RawLayoutService/Dockerfile
    ports:
      - "8081:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      # Set Cognito dev user pool values here or via a .env file.
      - Cognito__Authority=${COGNITO_AUTHORITY:-}
      - Cognito__Audience=${COGNITO_AUDIENCE:-}
      # DynamoDB settings for LocalStack
      - DynamoDB__ServiceUrl=http://localstack:4566
      - DynamoDB__TableName=RawLayouts
      - DynamoDB__Region=us-east-1
      # SQS settings for LocalStack
      - Sqs__ServiceUrl=http://localstack:4566
      - Sqs__QueueUrl=http://sqs.us-east-1.localhost.localstack.cloud:4566/000000000000/LayoutProcessingQueue
      - Sqs__Region=us-east-1
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
      - LocalStack__BaseUrl=http://localstack:4566
    depends_on:
      - localstack
    networks:
      - backend

  layoutcompilerservice:
    build:
      context: .
      dockerfile: src/AdaptiveRemote.Backend.LayoutCompilerService/Dockerfile
    ports:
      - "8083:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
    networks:
      - backend

  layoutprocessingservice:
    build:
      context: .
      dockerfile: src/AdaptiveRemote.Backend.LayoutProcessingService/Dockerfile
    ports:
      - "8082:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
      - ASPNETCORE_URLS=http://+:8080
      # Set Cognito dev user pool values here or via a .env file.
      - Cognito__Authority=${COGNITO_AUTHORITY:-}
      - Cognito__Audience=${COGNITO_AUDIENCE:-}
      # SQS settings for LocalStack
      - Sqs__ServiceUrl=http://localstack:4566
      - Sqs__QueueUrl=http://sqs.us-east-1.localhost.localstack.cloud:4566/000000000000/LayoutProcessingQueue
      - Sqs__Region=us-east-1
      # Upstream service URLs (Docker Compose DNS)
      - RawLayoutService__BaseUrl=http://rawlayoutservice:8080
      - CompiledLayoutService__BaseUrl=http://compiledlayoutservice:8080
      - LayoutCompilerService__BaseUrl=http://layoutcompilerservice:8080
      - AWS_ACCESS_KEY_ID=test
      - AWS_SECRET_ACCESS_KEY=test
      - LocalStack__BaseUrl=http://localstack:4566
    depends_on:
      - localstack
      - rawlayoutservice
      - compiledlayoutservice
      - layoutcompilerservice
    networks:
      - backend

networks:
  backend:
    driver: bridge

volumes:
  localstack-data:
