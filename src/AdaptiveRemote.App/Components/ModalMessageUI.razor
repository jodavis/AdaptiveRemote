@inherits Mvvm.MvvmComponent<Models.ModalMessageView>

@if (CurrentMessage is not null)
{
    <div class="conversation-speaking-message"><span>"@CurrentMessage"</span></div>
}

@code {
    private string? CurrentMessage => ViewModel?.CurrentMessage;
}
