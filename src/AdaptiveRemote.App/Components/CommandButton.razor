@inherits CommandButtonBase

<button class="@CssClasses" id="@ID" disabled=@(!IsEnabled) @onclick="Invoke">@Label</button>

@code {
    protected override bool IsEnabled => Command?.ExecuteAsync is not null && Command?.IsEnabled == true;

    protected override void Invoke()
    {
        Command?.ExecuteAsync?.Invoke(default);
    }
}
