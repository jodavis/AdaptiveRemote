<Window x:Class="AdaptiveRemote.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:blazor="clr-namespace:Microsoft.AspNetCore.Components.WebView.Wpf;assembly=Microsoft.AspNetCore.Components.WebView.Wpf"
        xmlns:components="clr-namespace:AdaptiveRemote.Components"
        mc:Ignorable="d"
        Title="MainWindow" WindowStyle="None" WindowState="Maximized"
        Background="#222">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <blazor:BlazorWebView Name="Browser" HostPage="wwwroot\index.html" Visibility="Hidden" Services="{DynamicResource services}" Grid.RowSpan="2">
            <blazor:BlazorWebView.RootComponents>
                <blazor:RootComponent Selector="#app" ComponentType="{x:Type components:Root}" />
            </blazor:BlazorWebView.RootComponents>
        </blazor:BlazorWebView>
        <TextBlock VerticalAlignment="Bottom" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold" FontSize="48" Grid.Row="0">Adaptive Remote</TextBlock>
        <DockPanel Grid.Row="1">
            <TextBlock DockPanel.Dock="Top" VerticalAlignment="Top" HorizontalAlignment="Center" Foreground="White" FontWeight="Bold" FontSize="36"  Text="{Binding TaskName}"></TextBlock>
            <StackPanel Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Center" Visibility="Collapsed">
                <Button Content="Show Error" Width="300" Height="72" Margin="10" FontSize="36" Click="ShowError_Click" />
                <Button Content="Exit" Width="300" Height="72" Margin="10" FontSize="36" Click="Exit_Click" />
            </StackPanel>
        </DockPanel>
        <Border Name="ErrorDisplay" Grid.RowSpan="2" BorderBrush="Red" Background="#222" BorderThickness="3" Margin="36" Visibility="Hidden">
            <DockPanel>
                <Button DockPanel.Dock="Top" Content="🗙" HorizontalAlignment="right" Background="#222" Foreground="Red" BorderThickness="0" FontSize="18" FontWeight="Bold" Margin="6" Click="ShowError_Click" />
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <TextBlock Foreground="Red" TextWrapping="Wrap" FontSize="16" Text="{Binding FatalError}" Margin="36" />
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</Window>
